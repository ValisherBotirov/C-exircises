#include<iostream>
#include<cmath>
using namespace std;
int main(){
	int y,x;
	cout<<"x ni qiymatini kiriting : ";
	cin>>x;
	y = sqrt(3*sin(1.5*pow(x,3)) + 1.6*pow(x,2)+2.7*x) / (abs(cos(log(sqrt(x+pow(sin(log10(x)),2))))));
	
	cout<<y;
	return 0;
}
